export default {
    setUpcomingMoviesData(state, movies) {
        state.upcomingMoviesData = movies
    },
    setCurrentMoviesData(state, movies) {
        state.currentMoviesData = movies
    },
}